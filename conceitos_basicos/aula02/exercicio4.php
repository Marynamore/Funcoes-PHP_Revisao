<?php

# definindo uma variável %x para armazenar um número inteiro.
$x = 5985;

# var_dump retorna o tipo e valor da variável.
var_dump($x);

# var_dump retorna se o valor da variável é inteiro.
var_dump(is_int($x));

# retorna se o valor máximo da variável interio.
var_dump(PHP_INT_MAX);

# retorna se o valor máximo da variável inteiro.
# var_dump(PHP_INT_MIN);
# var_dump retorna se o valor da variavel string é inteiro.
$x = "5985";
var_dump($x);
var_dump(is_int($x));
